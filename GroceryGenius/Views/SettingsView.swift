import SwiftUI

struct SettingsView: View {
    var body: some View {
        Text("Settings Screen")
            .font(.title)
            .foregroundColor(AppColor.primary)
            .padding()
    }
}

#Preview {
    SettingsView()
}
